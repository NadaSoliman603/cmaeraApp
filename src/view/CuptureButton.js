
import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const CuptureButton = (props) => {
    return (
        <View style={styles.screen}>
            <Text>CuptureButton</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    
});

export default CuptureButton;